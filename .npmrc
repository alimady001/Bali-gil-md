# Use modern flag to omit devDependencies instead of the deprecated `production` config.
# This sets npm's default to --omit=dev during installs in this project.
omit=dev

# Ensure project-level production config is not set.
production=false
